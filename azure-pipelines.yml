trigger:
  branches:
    include:
      - '*'
pr:
  branches:
    include:
      - '*'

pool:
  vmImage: 'windows-latest'

steps:
- checkout: self

- script: |
    git config user.name "Gabriel Paixão"
    git config user.email "gabrielpaixao588@gmail.com"
    
    git remote add azure-devops https://BREUPX-ORG@dev.azure.com/BREUPX-ORG/Learn-Project/_git/Addin

    git push azure-devops --all
    git push azure-devops --tags
  displayName: 'Mirror changes to Azure DevOps repository'
